apiVersion: v1
kind: Service
metadata:
  name: legacy-maliev-intranet
  namespace: maliev-legacy
spec:
  selector:
    app.kubernetes.io/name: legacy-maliev-intranet
  ports:
    - name: http
      port: 80
      targetPort: http

