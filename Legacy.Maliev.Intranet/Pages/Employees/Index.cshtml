@page
@model Legacy.Maliev.Intranet.Pages.Employees.IndexModel
@{
    ViewData["Title"] = "Employees";
}
<section class="panel">
    <p class="eyebrow">Employee profiles</p>
    <div class="page-heading">
        <h1>Employees</h1>
        <a class="button" asp-page="/Employees/Create">Create employee</a>
    </div>
    <form method="get" class="filters">
        <label>Search <input asp-for="Search" type="search" autocomplete="off" /></label>
        <label>Sort
            <select asp-for="Sort" asp-items="Html.GetEnumSelectList<Legacy.Maliev.Intranet.Employees.EmployeeSortType>()"></select>
        </label>
        <label>Per page <input asp-for="Size" type="number" min="1" max="250" /></label>
        <button type="submit">Apply</button>
    </form>
    @if (Model.Results.Items.Count == 0)
    {
        <p>No employees matched this query.</p>
    }
    else
    {
        <div class="table-scroll">
            <table>
                <thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Role</th><th></th></tr></thead>
                <tbody>
                @foreach (var employee in Model.Results.Items)
                {
                    <tr>
                        <td>@employee.Id</td>
                        <td>@employee.FullName</td>
                        <td>@employee.Email</td>
                        <td>@employee.Role?.Name</td>
                        <td><a asp-page="/Employees/View" asp-route-id="@employee.Id">View</a></td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
        <nav class="pager" aria-label="Employee pages">
            @if (Model.Results.HasPreviousPage)
            {
                <a asp-route-index="@(Model.Results.PageIndex - 1)" asp-route-size="@Model.Size" asp-route-search="@Model.Search" asp-route-sort="@Model.Sort">Previous</a>
            }
            <span>Page @Model.Results.PageIndex of @Model.Results.TotalPages · @Model.Results.TotalRecords records</span>
            @if (Model.Results.HasNextPage)
            {
                <a asp-route-index="@(Model.Results.PageIndex + 1)" asp-route-size="@Model.Size" asp-route-search="@Model.Search" asp-route-sort="@Model.Sort">Next</a>
            }
        </nav>
    }
</section>
